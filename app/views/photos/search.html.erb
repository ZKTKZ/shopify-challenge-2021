<body>
    <%= form_with html: { style: "width: 50%; margin: 0 auto; text-align: center;" }, url: "/photos/search", method: :get do |form| %>
      <%= form.label :query, "Search by tag:" %>
      <%= form.text_field :query %>
      <%= form.submit "Submit" %>
    <% end %>
</body>
